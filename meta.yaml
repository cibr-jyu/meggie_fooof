package:
  name: meggie_fooof
  version: "0.1.1"

source:
  path: .

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - meggie>=1.0.0
    - fooof>=1.0.0

about:
  license: BSD (3-clause)
  summary: 'Meggie plugin providing interface to fooof package'
